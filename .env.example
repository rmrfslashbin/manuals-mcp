# Manuals MCP Server Configuration
# Copy this file to .env and customize for your environment

# Database path (required for serve command)
MANUALS_DB_PATH=./data/manuals.db

# Documentation directory (required for index command)
MANUALS_DOCS_PATH=../manuals-data

# Logging configuration
MANUALS_LOG_LEVEL=info       # debug, info, warn, error
MANUALS_LOG_FORMAT=text      # text or json
MANUALS_LOG_OUTPUT=stderr    # stderr, /path/to/file, or /path/to/dir/

# Optional: Override with command-line flags
# Example: manuals-mcp serve --db-path=/custom/path/manuals.db
